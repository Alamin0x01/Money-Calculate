// calculate button

const calculateBtn = document.getElementById('calculate-button');
calculateBtn.addEventListener('click', calculateExpanseAndBalance);

// save button
const saveBtn = document.getElementById('save-button');
saveBtn.addEventListener('click', saveMoneyFromIncome);